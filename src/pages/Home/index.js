import React from 'react';
import { StyleSheet, View } from 'react-native';

const Home = (props) => {
    return(
        <View>

        </View>
    );
}

export default Home;

const styles = StyleSheet.create({
    
});